// const squared=(arr)=>(
//   arr.map((val)=>(
//   val*val))
// )

// This is a simple problem in which you have an array of numbers
function(arr){
let mapped=arr.map((val)=>(val*val))
return mapped
}
